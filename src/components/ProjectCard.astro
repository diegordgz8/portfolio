---
import Image from 'astro/components/Image.astro';
import type { ImageMetadata } from 'astro';
import { Icon } from 'astro-icon/components';
import type { Lang } from '../i18n/ui';
import { localizePath } from '../i18n/utils';

interface Props {
    name: string;
    description: string;
    slug: string;
    stack: string[];
    cover?: ImageMetadata;
    link?: string;
    pictures?: ImageMetadata[];
    github?: string;
    lang: Lang;
}

const { name, description, slug, stack, cover, link, github, lang } = Astro.props;
---

<article class="transition-transform duration-500 lg:hover:scale-105">
    <div class="relative overflow-hidden">
        {
            cover && (
                <a href={localizePath(`/portfolio/${slug}`, lang)}>
                    <Image
                        class="aspect-video w-full rounded-xl object-cover object-top shadow-lg"
                        src={cover}
                        alt={name}
                        transition:name={`img-${slug}`}
                    />
                </a>
            )
        }
        <div class="absolute right-2 bottom-2 flex gap-2">
            {
                github && (
                    <a
                        class="bg-primary-500 h-12 w-12 rounded-full p-2 shadow-lg"
                        href={github}
                        target="_blank"
                        rel="noopener noreferrer"
                    >
                        <Icon name="mdi:github" class="h-full w-full text-white" />
                    </a>
                )
            }

            {
                link && (
                    <a
                        class="bg-primary-500 grid h-12 w-12 place-content-center rounded-full p-2 text-center shadow-lg"
                        href={link}
                        target="_blank"
                        rel="noopener noreferrer"
                    >
                        <Icon name="mdi:link" class="h-6 w-6 text-white" />
                    </a>
                )
            }
        </div>
    </div>

    <div class="mt-4 flex flex-wrap gap-2 font-bold">
        {
            stack.map((item) => (
                <span class="bg-muted-500 rounded-full px-3 py-1 text-sm text-white md:px-4">
                    {item}
                </span>
            ))
        }
    </div>
    <h2 class="mt-4 text-3xl font-bold lg:text-4xl">{name}</h2>
    <p class="mt-2 opacity-70">{description}</p>
</article>
